# Melt 구현 로드맵

## 📊 현재 상태 평가

### ✅ 완료된 기능 (Phase 0 - MVP)
- 인증 및 온보딩
- 메시징 시스템 (DM, 공개 메시지, RT)
- 후원 시스템 (Intent, OCCURRED, CONFIRMED)
- 채널 및 검색
- 인기 크리에이터 목록
- 크리에이터 공개 메시지

### ✅ Phase 1.1 완료 (오늘 구현)
1. ✅ 읽지 않은 메시지 수 표시
2. ✅ 후원 완료 후 메시지 자동 등록
3. ✅ 프로필 설정 기능 (이름 변경, 로그아웃)

## 🎯 다음 구현 순서

### Phase 1.2 - 단기 구현 (1-2주)

#### 1. 메시지 상태 표시 ⭐⭐
**예상 시간: 2-3시간**

**구현 내용:**
- 메시지 읽음/안읽음 표시
- 전송 중/전송 완료/전송 실패 상태
- Mock 데이터로 테스트 가능

**Mock 데이터:**
```typescript
// 메시지에 read 상태 추가
mockMessages: [
  { ...message, read: false, sent: true }
]
```

**파일 수정:**
- `web/components/Messenger.tsx` - 상태 표시 추가
- `web/lib/mockData.ts` - Mock 메시지에 상태 추가

---

#### 2. 크리에이터 대시보드 통계 ⭐⭐
**예상 시간: 4-5시간**

**구현 내용:**
- API 엔드포인트: `GET /creator/stats?period=day|week|month`
- 기본 통계 표시 (총 후원액, 후원 건수)
- 기간별 필터
- Mock 데이터로 테스트 가능

**Mock 데이터:**
```typescript
'/creator/stats': (period: string) => ({
  data: {
    totalAmount: 150000,
    totalCount: 12,
    period,
    dailyStats: [...],
  }
})
```

**파일 수정:**
- `web/lib/mockData.ts` - Mock 통계 데이터 추가
- `web/lib/api.ts` - API 라우팅 추가
- `web/app/app/creator/dashboard/page.tsx` - 통계 UI 구현

---

#### 3. 실시간 피드 업데이트 개선 ⭐
**예상 시간: 2-3시간**

**구현 내용:**
- 새 메시지 자동 스크롤 개선
- 새 메시지 알림 배지
- Mock 데이터로 테스트 가능

**파일 수정:**
- `web/components/Messenger.tsx` - 업데이트 로직 개선

---

### Phase 2 - 중기 구현 (1개월)

#### 4. 뱃지 시스템
- 누적 치즈 기반 뱃지 티어
- 뱃지 표시 (프로필, 메시지)
- Mock 데이터로 테스트 가능

#### 5. 메시지 모더레이션
- 메시지 신고 기능
- 유저 차단 기능
- 스팸/도배 방지
- Mock 데이터로 테스트 가능

#### 6. 고급 통계 및 분석
- 시간대별 히트맵
- Top Supporters 목록
- 후원 내역 Export
- Mock 데이터로 테스트 가능

---

## 🚀 다음 단계: Phase 1.2 구현

### 우선순위 1: 메시지 상태 표시

**구현 계획:**
1. Mock 메시지에 `read`, `sent` 상태 추가
2. Messenger 컴포넌트에 상태 표시 UI 추가
3. 개발 모드에서 테스트

**테스트 시나리오:**
- 읽지 않은 메시지 표시 확인
- 전송 중/완료 상태 확인
- 크리에이터가 메시지 읽음 처리 확인

---

### 우선순위 2: 크리에이터 대시보드 통계

**구현 계획:**
1. Mock 통계 데이터 생성
2. API 엔드포인트 추가
3. 대시보드 UI 구현
4. 기간별 필터 추가
5. 개발 모드에서 테스트

**테스트 시나리오:**
- 크리에이터로 로그인
- 대시보드에서 통계 확인
- 기간별 필터 테스트

---

## 📝 구현 원칙

### 개발 모드 우선
- 모든 기능은 먼저 Mock 데이터로 구현
- 개발 모드에서 완전히 테스트 가능하도록
- 백엔드 API는 Mock 응답과 동일한 형태로 구현

### 점진적 개선
- Phase별로 기능 추가
- 각 Phase 완료 후 테스트 및 검증
- 사용자 피드백 반영

### 테스트 가능성
- Mock 데이터로 모든 시나리오 테스트
- 실제 백엔드 연결 없이도 작동
- 개발 속도 향상

## 🔄 Git 워크플로우

1. 기능 브랜치 생성
2. Mock 데이터로 구현
3. 개발 모드에서 테스트
4. 커밋 및 푸시
5. PR 생성 (선택적)

## 📚 참고 문서

- `PROJECT_EVALUATION.md` - 프로젝트 평가
- `IMPLEMENTATION_PLAN.md` - 상세 구현 계획
- `ADDITIONAL_FEATURES.md` - 추가 기능 목록
