# 빠른 시작 가이드

## 🚀 프로덕션 모드 (모든 페이지 미리 생성)

### 1. 빌드
```powershell
cd C:\Users\alex1\cheese3\web
npm run build
```

### 2. 서버 시작
```powershell
npm run start
```

### 3. 브라우저 접속
- http://localhost:3000
- 모든 페이지가 미리 생성되어 매우 빠름!

## ⚡ 개발 모드 (빠른 개발)

### Mock 모드로 개발
```powershell
cd C:\Users\alex1\cheese3\web
npm run dev:mock
```

## 📊 빌드 결과

### 정적 페이지 (○ 표시)
다음 페이지들은 빌드 시 미리 생성되어 즉시 로드:
- `/` - 홈
- `/app` - 메인 대시보드
- `/app/admin/*` - 관리자 페이지들
- `/app/conversations` - 대화방 목록
- `/app/search` - 검색
- `/help`, `/contact`, `/terms`, `/privacy` - 기타 페이지들

### 동적 페이지 (λ 표시)
다음 페이지들은 런타임에 렌더링 (파라미터 필요):
- `/app/channels/[chzzkChannelId]` - 채널별 페이지
- `/app/channels/[chzzkChannelId]/donate` - 후원 페이지

## 🎯 성능 개선

### Before
- 각 페이지 요청 시마다 서버 렌더링
- 첫 로딩: 500-1000ms

### After
- 빌드 시 미리 생성된 HTML 제공
- 첫 로딩: 50-100ms
- 페이지 전환: 50-100ms (클라이언트 사이드 라우팅)

## 💡 팁

1. **개발 중**: `npm run dev:mock` 사용 (빠른 재시작)
2. **테스트**: `npm run build && npm run start` 사용 (프로덕션과 동일)
3. **배포**: 빌드된 `.next` 폴더를 배포
